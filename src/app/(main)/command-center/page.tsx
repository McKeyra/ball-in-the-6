import { CommandCenterPage } from '@/views/command-center/CommandCenterPage';

export const metadata = {
  title: 'Command Center | Ball in the 6',
  description: 'Manage every aspect of your organization',
};

export default function Page(): React.ReactElement {
  return <CommandCenterPage />;
}
