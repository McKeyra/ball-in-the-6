import { ParentHubPage } from '@/views/parent-hub/ParentHubPage';

export const metadata = {
  title: 'Parent Hub | Ball in the 6',
  description: 'Everything you need to manage your family\'s sports activities',
};

export default function Page(): React.ReactElement {
  return <ParentHubPage />;
}
